apiVersion: v1
kind: Service
metadata:
  name: service-app
  labels:
    tier: rp

spec:
  type: ClusterIP
  selector:
    app: front
    
  ports:
    - name: http
      port: 8080
      targetPort: 80